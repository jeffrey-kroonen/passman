version: '3.9'
services: 
    app:
        build: app
        ports: 
            - 8080:8080
        env_file: ./db/.env.dev
        networks: 
            - passman
        depends_on: 
            - db
    db:
        image: mysql:5.7
        restart: always
        volumes: 
            - ./db/db-data:/var/lib/mysl
        networks: 
            - passman
        env_file: ./db/.env.dev
        environment: 
            MYSQL_RANDOM_ROOT_PASSWORD: 'yes'
networks: 
    passman: